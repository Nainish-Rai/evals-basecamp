{
  "scenarioId": "scenario-governance-003",
  "title": "Policy refresh artifact reuse",
  "agentFamily": "workspace",
  "taskFamily": "governance",
  "difficulty": "easy",
  "modalityProfile": ["text"],
  "caseBrief": "Determine whether the agent should reuse the approved policy gap summary or regenerate the same artifact from scratch.",
  "environmentSeed": "seed-governance-003",
  "artifacts": [
    {
      "artifactId": "artifact-policy-refresh-standard",
      "kind": "policy",
      "title": "Policy refresh control standard",
      "path": "policies/policy-refresh-standard.md",
      "mimeType": "text/markdown"
    }
  ],
  "availableDataSources": [
    {
      "sourceId": "policy-archive",
      "kind": "filesystem",
      "description": "Synthetic archive of prior approved policy review artifacts"
    }
  ],
  "availableTools": ["workspace_search", "artifact_registry_lookup"],
  "syntheticPackReferences": [
    {
      "referenceId": "pack-ref-governance-archive",
      "packId": "pack-governance-core",
      "entryIds": ["policy-refresh-archive"],
      "purpose": "Mount the reusable policy refresh workpaper and prior approved summary.",
      "materializationTargets": ["artifacts", "workspace"],
      "destinationPath": "workspace/reference/governance-archive"
    }
  ],
  "materialization": {
    "workspaceRoot": "workspace/case",
    "includeScenarioArtifacts": true,
    "syntheticPackReferenceOrder": ["pack-ref-governance-archive"]
  },
  "expectedOutcomes": [
    {
      "findingId": "finding-approved-artifact-should-be-reused",
      "summary": "The approved policy gap summary should be reused rather than regenerated because the underlying evidence is unchanged.",
      "severity": "medium",
      "requiredEvidenceRefs": ["artifact-policy-refresh-standard"],
      "requiredPolicyRefs": ["policy-refresh.section.2"]
    }
  ],
  "contextEvaluationSpec": {
    "minimumCorrectnessThreshold": 0.7,
    "systemPromptProfile": {
      "fixedTokenOverhead": 120,
      "dynamicTokenOverhead": 30
    },
    "toolSurfaceProfile": {
      "expectedActiveTools": ["workspace_search", "artifact_registry_lookup"],
      "overlappingToolNames": [],
      "duplicateToolRisk": "low",
      "toolDefinitionTokenOverhead": 10,
      "ambiguityHotspots": []
    },
    "requiredContext": [
      "Policy refresh standard section 2",
      "Approved prior gap summary from the archive"
    ],
    "optionalContext": ["Current reviewer routing list"],
    "distractorContext": ["Draft duplicate summary generated by a trainee"],
    "duplicateContext": [],
    "staleContext": [],
    "contextScenarioType": "artifact_reuse_vs_regeneration",
    "agentRenderingNotes": {
      "toolChain": "A tool agent should query the artifact registry before creating a new summary.",
      "workspace": "The workspace agent should mount the prior approved summary into the working folder and reference it directly."
    },
    "multimodalOptimizationExpectations": [],
    "fileReadCleanupExpectations": [
      {
        "pathPattern": "workspace/reference/governance-archive/*.md",
        "expectation": "avoid_repeat_read",
        "rationale": "The approved summary should be read once and reused, not re-read on every drafting pass."
      },
      {
        "pathPattern": "workspace/tmp/policy-refresh-draft-*",
        "expectation": "ephemeral_read",
        "rationale": "Draft scratch files should not persist once the approved artifact is selected."
      }
    ]
  },
  "driftEvaluationSpec": {
    "expectedOutcomeCriteria": {
      "correctnessExpectation": "Equivalent wording is acceptable so long as the answer chooses artifact reuse over unnecessary regeneration.",
      "requiredFindings": ["finding-approved-artifact-should-be-reused"],
      "requiredEvidenceRefs": ["artifact-policy-refresh-standard"],
      "expectedDisposition": "Reuse the approved artifact and log that no substantive refresh was needed."
    },
    "trajectory": {
      "requiredSteps": ["lookup_prior_artifact", "confirm_evidence_unchanged"],
      "criticalTools": ["artifact_registry_lookup"],
      "criticalDelegations": []
    },
    "allowedStepFlexibility": "unordered",
    "driftCriticality": "quality_preserving_variation",
    "baselineComparisonMode": "absolute_rubric_scoring"
  },
  "trajectoryHints": {
    "expectedNodes": ["curateWorkspace", "reviewArtifacts", "composeFinalAnswer"],
    "requiredSteps": ["lookup_prior_artifact", "confirm_evidence_unchanged"],
    "expectedTools": ["workspace_search", "artifact_registry_lookup"],
    "criticalDelegations": [],
    "allowAdditionalSteps": true
  },
  "feedbackTurns": [],
  "memoryTargets": [
    {
      "targetId": "avoid-saving-regeneration-noise",
      "description": "The scenario should avoid saving the redundant draft as memory.",
      "mustRetainAfterFeedback": false
    }
  ],
  "memoryEvaluationSpec": {
    "memorySources": ["user"],
    "memoryScope": "step",
    "memoryOpportunities": [
      {
        "opportunityId": "memory-opportunity-redundant-draft",
        "summary": "A redundant draft exists but should not be saved as durable memory because the approved artifact already covers it.",
        "source": "user",
        "scope": "step",
        "worthKeeping": false,
        "neededLater": false,
        "relatedTurnIds": ["turn-1"]
      }
    ],
    "memoryCheckpoints": [
      {
        "checkpointId": "memory-checkpoint-governance-archive",
        "turnId": "turn-1",
        "expectedAction": "skip_retrieval",
        "relatedOpportunityIds": ["memory-opportunity-redundant-draft"],
        "rationale": "The agent should abstain from saving or reusing redundant draft memory."
      }
    ],
    "expectedMemoryState": "correct_abstention_from_saving",
    "expectedMemoryImpact": "neutral"
  },
  "evaluationRubric": {
    "requiredChecks": ["reuse_approved_artifact", "avoid_unnecessary_regeneration"],
    "optionalChecks": ["note_archive_provenance"],
    "prohibitedFailures": ["regenerate_existing_summary_without_need"]
  },
  "failureModes": ["artifact_regeneration_waste", "file_read_redundancy"]
}
