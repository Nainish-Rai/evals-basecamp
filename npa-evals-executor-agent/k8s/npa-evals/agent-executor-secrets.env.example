# Copy this file to agent-executor-secrets.env before running kubectl apply -k.
# Do not commit the populated file.
OPENAI_API_KEY=replace-me
MINIO_ACCESS_KEY=replace-me
MINIO_SECRET_KEY=replace-me
WORKSPACE_AGENT_API_KEYS=replace-me-with-a-long-random-api-key
