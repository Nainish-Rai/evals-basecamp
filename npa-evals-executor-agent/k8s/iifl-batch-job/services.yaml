apiVersion: v1
kind: Service
metadata:
  name: agent-executor
  labels:
    app.kubernetes.io/name: agent-executor
    app.kubernetes.io/part-of: workspace-agent
spec:
  selector:
    app.kubernetes.io/name: agent-executor
  ports:
    - name: http
      port: 8010
      targetPort: http-agent
